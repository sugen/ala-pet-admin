import { EntityPage } from "@/components/entity-page";

export default function CrawlTasksPage() {
  return <EntityPage entity="crawl-tasks" title="采集任务" description="管理来源采集、关键词采集、美容采集和 RSS 采集任务。" />;
}