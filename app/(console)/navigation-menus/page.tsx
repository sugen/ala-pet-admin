import { EntityPage } from "@/components/entity-page";

export default function NavigationMenusPage() {
  return <EntityPage entity="navigation-menus" title="前台菜单管理" description="配置前台一级菜单、路径、排序和 is_visible 显隐开关，不保留旧二级菜单。" />;
}